import React from 'react';
import { Header } from './styles';

const Title = () => {
  return (
    <Header className="header">
      <h2>Weather App</h2>
    </Header>
  );
};

export default Title;
